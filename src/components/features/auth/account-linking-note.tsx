const AccountLinkingNote = () => {
    return (
        <p className="rounded-md border border-border bg-muted/40 px-3 py-2 text-center text-muted-foreground text-xs leading-relaxed">
            Use the same email for Google and password sign-in to keep one
            account.
        </p>
    );
};

export default AccountLinkingNote;
